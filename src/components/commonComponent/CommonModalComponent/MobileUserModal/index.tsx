import ModalPresenter from "./ModalPresenter";

const MobileUserModal = (props: any) => {

    return (
        <ModalPresenter
            modalProps={props}
        />
    )
}
export default MobileUserModal;