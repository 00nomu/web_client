import { red } from '@mui/material/colors';
import { makeStyles  } from '@mui/styles';
import { isAfter } from 'date-fns';
import Theme from "../../../../styles/Theme";

export const  mInput2 = makeStyles({
  
  test : {
    fontSize : 8,
  },
  borderEvent : {
    
      border : "1px solid blue"
   
  }
    

});
